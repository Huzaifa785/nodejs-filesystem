{
  "name": "nodejs-filesystem",
  "version": "1.0.0",
  "description": "A basic project that creates a file in a particular folder on hitting the create endopint and retrieves all the files from that particular folder.",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "nodejs",
    "filesystem"
  ],
  "author": "Huzaifa  Mohammed",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.0.1",
    "express": "^4.18.1"
  },
  "engines": {
    "node": "16.x"
  }
}
